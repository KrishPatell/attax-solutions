/** Cookie + accessibility FABs: keep corners symmetric (same inset from bottom / side). */
export const FAB_CORNER_INSET_PX = 20;
export const FAB_TRIGGER_SIZE_PX = 48;
/** Vertical gap between trigger top and panel bottom when panel is open */
export const FAB_PANEL_GAP_PX = 16;

export const fabPanelBottomPx =
  FAB_CORNER_INSET_PX + FAB_TRIGGER_SIZE_PX + FAB_PANEL_GAP_PX;
